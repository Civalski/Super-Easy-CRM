# Verificar Tipos (TypeScript)
echo "🔍 Verificando TypeScript..."
npx tsc --noEmit --pretty
if [ $? -ne 0 ]; then
    echo "❌ Erro de TypeScript encontrado! Corrija antes de commitar."
    exit 1
fi

# Linting nos arquivos staged
echo "🧹 Rodando Lint..."
npx lint-staged
