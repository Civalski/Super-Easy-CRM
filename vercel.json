{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run db:deploy && npm run build",
  "crons": [
    {
      "path": "/api/prospectos/agendamentos/processar",
      "schedule": "*/5 * * * *"
    }
  ]
}
